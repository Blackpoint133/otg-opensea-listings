import test from "node:test";
import assert from "node:assert/strict";
import type { DbPool, QueryResult, TransactionClient } from "../src/db/types.js";
import { InMemoryTargetedVerifierAttemptStore, PostgresTargetedVerifierAttemptStore, operationalSemanticEvidenceHash, type OperationalAttemptRecord } from "../src/reconciliation/verifier/targetedVerifierOperationalWorker.js";
import { sha256Canonical } from "../src/reconciliation/evidence/canonicalEvidence.js";
import { OPENSEA_ORDER_CONTRACT_VERSION, TARGETED_VERIFIER_CANDIDATE_MODEL_VERSION, TARGETED_VERIFIER_ENDPOINT_PATH, TARGETED_VERIFIER_GENERATION_MODEL_VERSION, TARGETED_VERIFIER_NORMALIZER_VERSION, TARGETED_VERIFIER_POLICY_VERSION, TARGETED_VERIFIER_SCHEMA_VERSION } from "../src/reconciliation/verifier/targetedVerifierTypes.js";

type Row={payload:OperationalAttemptRecord;lifecycle:string;claimedAt:string|null;leaseExpiresAt:string|null;leaseToken:string|null;nextAttemptAt:string|null;failureClassification:string|null};
const clone=<T>(x:T):T=>JSON.parse(JSON.stringify(x));
const rec=(id="a",lifecycle:OperationalAttemptRecord["lifecycle"]="NOT_STARTED"):OperationalAttemptRecord=>({attemptId:sha256Canonical({id}),attemptNumber:0,idempotencyKey:sha256Canonical({key:id}),sweepId:"s",orderHash:"0x"+"a".repeat(64),candidateArtifactHash:"a".repeat(64),barrierArtifactHash:"b".repeat(64),generationRootHash:"c".repeat(64),candidateModelVersion:TARGETED_VERIFIER_CANDIDATE_MODEL_VERSION,generationModelVersion:TARGETED_VERIFIER_GENERATION_MODEL_VERSION,verifierSchemaVersion:TARGETED_VERIFIER_SCHEMA_VERSION,verifierPolicyVersion:TARGETED_VERIFIER_POLICY_VERSION,providerContractVersion:OPENSEA_ORDER_CONTRACT_VERSION,normalizerVersion:TARGETED_VERIFIER_NORMALIZER_VERSION,expectedIdentity:{orderHash:"0x"+"a".repeat(64),chain:"gunzilla",contractAddress:"0x"+"b".repeat(40),tokenId:"1",collectionSlug:"x",protocolAddress:"0x"+"d".repeat(40)},requestIdentity:{method:"GET",endpointPath:TARGETED_VERIFIER_ENDPOINT_PATH,chain:"gunzilla",protocolAddress:"0x"+"d".repeat(40),orderHash:"0x"+"a".repeat(64)},lifecycle,leaseToken:null,createdAt:"2026-01-01T00:00:00.000Z",claimedAt:null,leaseExpiresAt:null,verificationStartedAt:null,verificationCompletedAt:null,providerObservedAt:null,httpStatus:null,transportOutcome:null,responseBodySha256:null,rawResponseArtifactHash:null,normalizedProviderStatus:null,providerResultStatus:null,finalResultStatus:null,providerReasonCodes:[],providerResultReasonCodes:[],normalizedOrder:null,reasonCodes:[],retry:{retryable:false,retryReason:"NONE",recommendedPolicyClass:"NONE"},preVerificationWatermark:{eventId:"0",receivedAt:"2026-01-01T00:00:00.000Z"},postVerificationWatermark:null,preRelevantFingerprint:{orderHash:"0x"+"a".repeat(64),eventIds:[],events:[],digest:"0".repeat(64),orderingAmbiguous:false},postRelevantFingerprint:null,semanticEvidenceHash:null,authorityGranted:false,deactivationAuthorityGranted:false,failureClassification:null,nextAttemptAt:null});
class AttemptDb implements DbPool{
 rows=new Map<string,Row>(); sql:string[]=[];
 resolve(id:string){return this.rows.get(id) ?? this.rows.get(sha256Canonical({id}));}
 async end(){} async connect():Promise<TransactionClient>{return {release(){},query:(q,v)=>this.query(q,v)}}
 private out<T>(rows:T[],rowCount=rows.length):QueryResult<T>{return {rows,rowCount};}
 async query<T=unknown>(q:string,v?:readonly unknown[]):Promise<QueryResult<T>>{const l=q.toLowerCase();this.sql.push(q);
  if(l.startsWith("insert into public.targeted_verifier_attempts")){const p=JSON.parse(String(v?.[11])) as OperationalAttemptRecord;const old=[...this.rows.values()].find(x=>x.payload.idempotencyKey===p.idempotencyKey);if(old)return this.out([{payload:clone(old.payload)} as T]);this.rows.set(p.attemptId,{payload:clone(p),lifecycle:p.lifecycle,claimedAt:p.claimedAt,leaseExpiresAt:p.leaseExpiresAt,leaseToken:p.leaseToken,nextAttemptAt:p.nextAttemptAt,failureClassification:p.failureClassification});return this.out([{payload:clone(p)} as T]);}
  if(l.startsWith("select payload from public.targeted_verifier_attempts where ((")){const now=Date.parse(String(v?.[0]));const rows=[...this.rows.values()].filter(r=>{const leaseFree=!r.leaseToken||!r.leaseExpiresAt||Date.parse(r.leaseExpiresAt)<=now;if(r.lifecycle==="NOT_STARTED")return (!r.nextAttemptAt||Date.parse(r.nextAttemptAt)<=now)&&leaseFree;if(r.lifecycle==="RESPONSE_OBSERVED"||r.lifecycle==="PENDING_FENCE")return leaseFree;return false;}).slice(0,Number(v?.[1]??99)).map(r=>({payload:clone(r.payload)} as T));return this.out(rows);}
  if(l.startsWith("select payload")){const r=this.resolve(String(v?.[0]));return this.out(r?[{payload:clone(r.payload)} as T]:[]);}
  if(l.startsWith("update public.targeted_verifier_attempts set lifecycle=case when lifecycle='request_pending'")){let n=0;const now=Date.parse(String(v?.[0]));for(const r of this.rows.values()){if(!r.leaseExpiresAt||Date.parse(r.leaseExpiresAt)>now||!["REQUEST_PENDING","RESPONSE_OBSERVED","PENDING_FENCE"].includes(r.lifecycle))continue;const old=r.lifecycle;r.lifecycle=old==="REQUEST_PENDING"?"FAILED":old;r.failureClassification=old==="REQUEST_PENDING"?"REQUEST_OUTCOME_UNCERTAIN":r.failureClassification;r.leaseToken=null;r.leaseExpiresAt=null;r.payload={...r.payload,lifecycle:r.lifecycle as OperationalAttemptRecord["lifecycle"],failureClassification:r.failureClassification,leaseToken:null,leaseExpiresAt:null};n++;}return this.out([],n);}
  if(l.startsWith("update public.targeted_verifier_attempts set lifecycle=case")){const r=this.resolve(String(v?.[0]));if(!r)return this.out([]);const now=Date.parse(String(v?.[1]));const dueNow=(!r.nextAttemptAt||Date.parse(r.nextAttemptAt)<=now);const leaseFree=!r.leaseToken||!r.leaseExpiresAt||Date.parse(r.leaseExpiresAt)<=now;const runnable=(r.lifecycle==="NOT_STARTED"&&dueNow&&leaseFree)||( (r.lifecycle==="RESPONSE_OBSERVED"||r.lifecycle==="PENDING_FENCE")&&leaseFree);if(!runnable)return this.out([]);const token=String(v?.[3]);r.lifecycle=r.lifecycle==="NOT_STARTED"?"REQUEST_PENDING":r.lifecycle;r.claimedAt=String(v?.[1]);r.leaseExpiresAt=String(v?.[2]);r.leaseToken=token;r.payload={...r.payload,lifecycle:r.lifecycle as OperationalAttemptRecord["lifecycle"],claimedAt:r.claimedAt,leaseExpiresAt:r.leaseExpiresAt,leaseToken:token,verificationStartedAt:r.payload.verificationStartedAt??r.claimedAt};return this.out([{payload:clone(r.payload)} as T]);}
  if(l.startsWith("update public.targeted_verifier_attempts set lifecycle=$3")){const r=this.resolve(String(v?.[0]));const now=Date.parse(String(v?.[9]));if(!r||!(r.lifecycle==="REQUEST_PENDING"||r.lifecycle==="RESPONSE_OBSERVED"||r.lifecycle==="PENDING_FENCE")||r.leaseToken!==String(v?.[1])||!r.leaseExpiresAt||Date.parse(r.leaseExpiresAt)<=now)return this.out([],0);const p=JSON.parse(String(v?.[3])) as OperationalAttemptRecord;r.payload=clone(p);r.lifecycle=p.lifecycle;r.claimedAt=p.claimedAt;r.leaseExpiresAt=p.leaseExpiresAt;r.leaseToken=p.leaseToken;r.nextAttemptAt=p.nextAttemptAt;r.failureClassification=p.failureClassification;return this.out([],1);}
  throw new Error("UNSUPPORTED_SQL:"+q);
 }}
const parity=(db:AttemptDb,id:string)=>{const r=db.resolve(id);assert.ok(r);assert.equal(r.lifecycle,r.payload.lifecycle);assert.equal(r.claimedAt,r.payload.claimedAt);assert.equal(r.leaseExpiresAt,r.payload.leaseExpiresAt);assert.equal(r.leaseToken,r.payload.leaseToken);assert.equal(r.nextAttemptAt,r.payload.nextAttemptAt);assert.equal(r.failureClassification,r.payload.failureClassification);};
const evidence=(r:OperationalAttemptRecord)=>({providerObservedAt:r.providerObservedAt,httpStatus:r.httpStatus,transportOutcome:r.transportOutcome,responseBodySha256:r.responseBodySha256,rawResponseArtifactHash:r.rawResponseArtifactHash,normalizedProviderStatus:r.normalizedProviderStatus,providerResultStatus:r.providerResultStatus,providerResultReasonCodes:r.providerResultReasonCodes,normalizedOrder:r.normalizedOrder,retry:r.retry});
test("Postgres attempt store create/get/claim/CAS parity",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const r=rec();assert.deepEqual(await s.createOrGet(r),r);assert.deepEqual(await s.createOrGet({...r,providerResultStatus:"MUTATED"}),r);parity(db,"a");const c=await s.claim("a","w","2026-01-01T00:00:00.000Z",1000);assert.ok(c);parity(db,"a");const before=clone(await s.get("a"));assert.equal(await s.complete("a","wrong",{...c.record,lifecycle:"COMPLETE"},"2026-01-01T00:00:00.100Z"),false);assert.deepEqual(await s.get("a"),before);assert.equal(await s.complete("a",c.leaseToken,{...c.record,lifecycle:"COMPLETE",leaseToken:null,leaseExpiresAt:null},"2026-01-01T00:00:00.100Z"),true);parity(db,"a");});
test("Postgres recovery/reclaim/CAS transitions preserve evidence and retry",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const o=rec("o","RESPONSE_OBSERVED");const rich={...o,leaseToken:"old",leaseExpiresAt:"2025-12-31T23:59:00.000Z",claimedAt:"2025-12-31T23:58:00.000Z",verificationStartedAt:"2025-12-31T23:00:00.000Z",providerObservedAt:"2025-12-31T23:01:00.000Z",httpStatus:429,transportOutcome:"HTTP",responseBodySha256:"eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee",rawResponseArtifactHash:"aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",normalizedProviderStatus:"INACTIVE",providerResultStatus:"INACTIVE_CONFIRMED",providerResultReasonCodes:["RATE_LIMITED"],retry:{retryable:true,retryReason:"RATE_LIMITED",recommendedPolicyClass:"RATE_LIMITED"}};await s.createOrGet(rich);const ev=evidence(rich);const c=await s.claim("o","new","2026-01-01T00:00:00.000Z",1000);assert.ok(c);assert.deepEqual(evidence((await s.get("o"))!),ev);parity(db,"o");assert.equal(await s.saveResponse("o","bad",rich,"2026-01-01T00:00:00.100Z"),false);assert.equal(await s.saveResponse("o",c.leaseToken,{...c.record,lifecycle:"RESPONSE_OBSERVED"},"2026-01-01T00:00:00.100Z"),true);parity(db,"o");assert.deepEqual((await s.get("o"))?.retry,rich.retry);const p=rec("p","PENDING_FENCE");p.leaseToken="p";p.leaseExpiresAt="2026-01-01T00:00:10.000Z";p.retry={retryable:true,retryReason:"TRANSIENT_TRANSPORT",recommendedPolicyClass:"TRANSIENT_TRANSPORT"};await s.createOrGet(p);assert.equal(await s.reclaimExpired("2026-01-01T00:00:11.000Z"),2);parity(db,"o");parity(db,"p");assert.equal((await s.get("p"))?.lifecycle,"PENDING_FENCE");assert.deepEqual((await s.get("p"))?.retry,p.retry);});
test("Postgres recovery claims preserve lifecycle, evidence and original verification time",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);for(const [id,lifecycle] of [["ro","RESPONSE_OBSERVED"],["pf","PENDING_FENCE"]] as const){const r={...rec(id,lifecycle),verificationStartedAt:"2025-12-31T23:00:00.000Z",providerObservedAt:"2025-12-31T23:01:00.000Z",providerResultStatus:"INACTIVE_CONFIRMED",retry:{retryable:true,retryReason:"RATE_LIMITED",recommendedPolicyClass:"RATE_LIMITED"},leaseToken:null,leaseExpiresAt:null};await s.createOrGet(r);const c=await s.claim(id,"recover","2026-01-01T00:00:00.000Z",1000);assert.ok(c);assert.equal(c.record.lifecycle,lifecycle);assert.equal(c.record.verificationStartedAt,r.verificationStartedAt);assert.equal(c.record.providerResultStatus,r.providerResultStatus);assert.deepEqual(c.record.retry,r.retry);parity(db,id);}});
test("Postgres listDue excludes active recovery leases",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);await s.createOrGet(rec("n"));await s.createOrGet(rec("o","RESPONSE_OBSERVED"));await s.createOrGet(rec("p","PENDING_FENCE"));assert.ok(await s.claim("o","w","2026-01-01T00:00:00.000Z",100000));const ids=(await s.listDue("2026-01-01T00:00:01.000Z",10)).map(x=>x.attemptId).sort();assert.deepEqual(ids,[sha256Canonical({id:"n"}),sha256Canonical({id:"p"})].sort());});
test("Postgres listDue full lifecycle matrix",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const rows=[{id:"n-due",l:"NOT_STARTED" as const},{id:"n-future",l:"NOT_STARTED" as const,next:"2026-01-02T00:00:00.000Z"},{id:"n-active",l:"NOT_STARTED" as const,token:"a",expiry:"2026-01-01T01:00:00.000Z"},{id:"n-expired",l:"NOT_STARTED" as const,token:"b",expiry:"2025-12-31T23:00:00.000Z"},{id:"o-free",l:"RESPONSE_OBSERVED" as const},{id:"o-active",l:"RESPONSE_OBSERVED" as const,token:"c",expiry:"2026-01-01T01:00:00.000Z"},{id:"o-expired",l:"RESPONSE_OBSERVED" as const,token:"d",expiry:"2025-12-31T23:00:00.000Z"},{id:"o-future",l:"RESPONSE_OBSERVED" as const,next:"2026-01-02T00:00:00.000Z"},{id:"p-free",l:"PENDING_FENCE" as const},{id:"p-active",l:"PENDING_FENCE" as const,token:"e",expiry:"2026-01-01T01:00:00.000Z"},{id:"p-expired",l:"PENDING_FENCE" as const,token:"f",expiry:"2025-12-31T23:00:00.000Z"},{id:"p-future",l:"PENDING_FENCE" as const,next:"2026-01-02T00:00:00.000Z"}];for(const x of rows){await s.createOrGet({...rec(x.id,x.l),nextAttemptAt:x.next??null,leaseToken:x.token??null,leaseExpiresAt:x.expiry??null});}const ids=(await s.listDue("2026-01-01T00:00:00.000Z",20)).map(x=>x.attemptId).sort();assert.deepEqual(ids,["n-due","n-expired","o-expired","o-free","o-future","p-expired","p-free","p-future"].map(id=>sha256Canonical({id})).sort());});
test("REQUEST_PENDING expiry fails closed in indexed and payload state",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const r=rec("u","REQUEST_PENDING");r.leaseToken="u-token";r.leaseExpiresAt="2026-01-01T00:00:00.100Z";r.claimedAt="2026-01-01T00:00:00.000Z";await s.createOrGet(r);assert.equal(await s.reclaimExpired("2026-01-01T00:00:01.000Z"),1);const got=await s.get("u");assert.equal(got?.lifecycle,"FAILED");assert.equal(got?.failureClassification,"REQUEST_OUTCOME_UNCERTAIN");assert.equal(got?.leaseToken,null);assert.equal(got?.leaseExpiresAt,null);parity(db,"u");});
test("Postgres CAS expiry, takeover, fail and retry parity",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);await s.createOrGet(rec("x","RESPONSE_OBSERVED"));const a=await s.claim("x","a","2026-01-01T00:00:00.000Z",1000);assert.ok(a);const before=clone(await s.get("x"));assert.equal(await s.fail("x",a.leaseToken,{...a.record,lifecycle:"FAILED",failureClassification:"EXECUTION_FAILED"},"2026-01-01T00:00:01.000Z"),false);assert.deepEqual(await s.get("x"),before);const b=await s.claim("x","b","2026-01-01T00:00:01.001Z",1000);assert.ok(b);assert.equal(await s.complete("x",a.leaseToken,{...b.record,lifecycle:"COMPLETE"},"2026-01-01T00:00:01.100Z"),false);assert.equal(await s.scheduleRetry("x",b.leaseToken,{...b.record,nextAttemptAt:"2026-01-01T00:10:00.000Z"},"2026-01-01T00:00:01.100Z"),true);parity(db,"x");});
test("Postgres successful fail clears both lease representations",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);await s.createOrGet(rec("f"));const c=await s.claim("f","w","2026-01-01T00:00:00.000Z",1000);assert.ok(c);const rich={...c.record,providerResultStatus:"INACTIVE_CONFIRMED",retry:{retryable:true,retryReason:"RATE_LIMITED",recommendedPolicyClass:"RATE_LIMITED"},failureClassification:"EXECUTION_FAILED"};assert.equal(await s.fail("f",c.leaseToken,{...rich,lifecycle:"FAILED",leaseToken:null,leaseExpiresAt:null},"2026-01-01T00:00:00.100Z"),true);const got=await s.get("f");assert.equal(got?.lifecycle,"FAILED");assert.equal(got?.failureClassification,"EXECUTION_FAILED");assert.equal(got?.leaseToken,null);assert.equal(got?.leaseExpiresAt,null);assert.equal(got?.providerResultStatus,"INACTIVE_CONFIRMED");assert.deepEqual(got?.retry,rich.retry);parity(db,"f");});
test("Postgres rejects future NOT_STARTED and terminal claims",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const future={...rec("future"),nextAttemptAt:"2026-01-02T00:00:00.000Z"};await s.createOrGet(future);assert.equal(await s.claim("future","w","2026-01-01T00:00:00.000Z",1000),null);for(const lifecycle of ["COMPLETE","FAILED"] as const){const r={...rec(lifecycle.toLowerCase(),lifecycle),leaseToken:"x",leaseExpiresAt:"2026-01-01T01:00:00.000Z"};await s.createOrGet(r);assert.equal(await s.claim(r.attemptId,"w","2026-01-01T00:00:00.000Z",1000),null);}});
test("Postgres CAS rejects matching-token terminal current rows",async()=>{for(const lifecycle of ["COMPLETE","FAILED"] as const){const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const r={...rec(lifecycle.toLowerCase(),lifecycle),leaseToken:"terminal-token",leaseExpiresAt:"2026-01-01T01:00:00.000Z"};db.rows.set(r.attemptId,{payload:clone(r),lifecycle,claimedAt:r.claimedAt,leaseExpiresAt:r.leaseExpiresAt,leaseToken:r.leaseToken,nextAttemptAt:r.nextAttemptAt,failureClassification:r.failureClassification});const before=clone(await s.get(r.attemptId));assert.equal(r.leaseToken,"terminal-token");assert.ok(Date.parse(r.leaseExpiresAt)>Date.parse("2026-01-01T00:00:00.000Z"));assert.equal(await s.saveResponse(r.attemptId,"terminal-token",{...r,lifecycle:"RESPONSE_OBSERVED"},"2026-01-01T00:00:00.000Z"),false);assert.deepEqual(await s.get(r.attemptId),before);parity(db,r.attemptId);}});
test("Postgres RESPONSE_OBSERVED reclaim preserves rich provider evidence",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const r={...rec("rich-observed","RESPONSE_OBSERVED"),leaseToken:"lease",leaseExpiresAt:"2026-01-01T00:00:00.100Z",claimedAt:"2026-01-01T00:00:00.000Z",verificationStartedAt:"2025-12-31T23:00:00.000Z",providerObservedAt:"2025-12-31T23:01:00.000Z",httpStatus:429,transportOutcome:"HTTP" as const,responseBodySha256:"cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc",rawResponseArtifactHash:"dddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd",normalizedProviderStatus:"INACTIVE",providerResultStatus:"INACTIVE_CONFIRMED",providerResultReasonCodes:["RATE_LIMITED"],reasonCodes:["RATE_LIMITED"],retry:{retryable:true,retryReason:"RATE_LIMITED",recommendedPolicyClass:"RATE_LIMITED"},failureClassification:"RETRY_SCHEDULED"};await s.createOrGet(r);const before=evidence(r),retry=clone(r.retry),started=r.verificationStartedAt;assert.equal(await s.reclaimExpired("2026-01-01T00:00:01.000Z"),1);const after=await s.get(r.attemptId);assert.equal(after?.lifecycle,"RESPONSE_OBSERVED");assert.equal(after?.leaseToken,null);assert.equal(after?.leaseExpiresAt,null);assert.deepEqual(evidence(after!),before);assert.deepEqual(after?.retry,retry);assert.equal(after?.verificationStartedAt,started);assert.equal(after?.failureClassification,r.failureClassification);parity(db,r.attemptId);});
test("Postgres PENDING_FENCE reclaim preserves rich provider and fence evidence",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const base=rec("rich-fence","PENDING_FENCE");const r={...base,leaseToken:"lease",leaseExpiresAt:"2026-01-01T00:00:00.100Z",claimedAt:"2026-01-01T00:00:00.000Z",verificationStartedAt:"2025-12-31T23:00:00.000Z",providerObservedAt:"2025-12-31T23:01:00.000Z",httpStatus:200,transportOutcome:"HTTP" as const,responseBodySha256:"cccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccccc",rawResponseArtifactHash:"dddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddddd",normalizedProviderStatus:"ACTIVE",providerResultStatus:"ACTIVE_CONFIRMED",providerResultReasonCodes:["ACTIVE"],reasonCodes:["ACTIVE"],retry:{retryable:true,retryReason:"TRANSIENT_TRANSPORT",recommendedPolicyClass:"TRANSIENT_TRANSPORT"},preVerificationWatermark:{eventId:"4",receivedAt:"2025-12-31T23:00:00.000Z"},postVerificationWatermark:{eventId:"5",receivedAt:"2025-12-31T23:01:00.000Z"},postRelevantFingerprint:{orderHash:base.orderHash,eventIds:[],events:[],digest:"1".repeat(64),orderingAmbiguous:false}};await s.createOrGet(r);const before=evidence(r),retry=clone(r.retry),started=r.verificationStartedAt,pre=clone(r.preVerificationWatermark),preFp=clone(r.preRelevantFingerprint),post=clone(r.postVerificationWatermark),postFp=clone(r.postRelevantFingerprint);assert.equal(await s.reclaimExpired("2026-01-01T00:00:01.000Z"),1);const after=await s.get(r.attemptId);assert.equal(after?.lifecycle,"PENDING_FENCE");assert.equal(after?.leaseToken,null);assert.equal(after?.leaseExpiresAt,null);assert.deepEqual(evidence(after!),before);assert.deepEqual(after?.retry,retry);assert.equal(after?.verificationStartedAt,started);assert.deepEqual(after?.preVerificationWatermark,pre);assert.deepEqual(after?.preRelevantFingerprint,preFp);assert.deepEqual(after?.postVerificationWatermark,post);assert.deepEqual(after?.postRelevantFingerprint,postFp);parity(db,r.attemptId);});

test("Postgres legacy payload policy and non-null final status parity",async()=>{const db=new AttemptDb();const s=new PostgresTargetedVerifierAttemptStore(db);const base=rec("legacy");const {finalResultStatus:_missing,...legacy}=base;db.rows.set("legacy",{payload:clone(legacy) as OperationalAttemptRecord,lifecycle:legacy.lifecycle,claimedAt:legacy.claimedAt,leaseExpiresAt:legacy.leaseExpiresAt,leaseToken:legacy.leaseToken,nextAttemptAt:legacy.nextAttemptAt,failureClassification:legacy.failureClassification});assert.equal((await s.get("legacy"))?.finalResultStatus,null);const hashed={...legacy,semanticEvidenceHash:"b".repeat(64)};db.rows.set("hashed",{payload:clone(hashed) as OperationalAttemptRecord,lifecycle:hashed.lifecycle,claimedAt:hashed.claimedAt,leaseExpiresAt:hashed.leaseExpiresAt,leaseToken:hashed.leaseToken,nextAttemptAt:hashed.nextAttemptAt,failureClassification:hashed.failureClassification});await assert.rejects(()=>s.get("hashed"),/LEGACY_OPERATIONAL_SEMANTIC_HASH_INCOMPATIBLE/);const put=(id:string,p:OperationalAttemptRecord)=>db.rows.set(id,{payload:clone(p),lifecycle:p.lifecycle,claimedAt:p.claimedAt,leaseExpiresAt:p.leaseExpiresAt,leaseToken:p.leaseToken,nextAttemptAt:p.nextAttemptAt,failureClassification:p.failureClassification});const current={...base,lifecycle:"PENDING_FENCE" as const,attemptId:sha256Canonical({id:"current"}),idempotencyKey:sha256Canonical({key:"current"}),providerResultStatus:"INACTIVE_CONFIRMED" as const,finalResultStatus:"RECONCILIATION_REQUIRED" as const,postVerificationWatermark:{eventId:"1",receivedAt:"2026-01-01T00:00:01.000Z"},postRelevantFingerprint:{...base.preRelevantFingerprint,eventIds:[],events:[],digest:base.preRelevantFingerprint.digest}};current.semanticEvidenceHash=operationalSemanticEvidenceHash(current);await s.createOrGet(current);const loaded=await s.get(sha256Canonical({id:"current"}));assert.equal(loaded?.providerResultStatus,"INACTIVE_CONFIRMED");assert.equal(loaded?.finalResultStatus,"RECONCILIATION_REQUIRED");assert.equal(loaded?.semanticEvidenceHash,operationalSemanticEvidenceHash(loaded!));parity(db,sha256Canonical({id:"current"}));put("tampered",{...current,attemptId:sha256Canonical({id:"tampered"}),idempotencyKey:sha256Canonical({key:"tampered"}),finalResultStatus:"STALE"});await assert.rejects(()=>s.get("tampered"),/OPERATIONAL_SEMANTIC_HASH_MISMATCH/);put("bad-hash",{...current,attemptId:sha256Canonical({id:"bad-hash"}),idempotencyKey:sha256Canonical({key:"bad-hash"}),semanticEvidenceHash:"z"});await assert.rejects(()=>s.get("bad-hash"),/INVALID_OPERATIONAL_SEMANTIC_HASH/);put("no-hash",{...current,attemptId:sha256Canonical({id:"no-hash"}),idempotencyKey:sha256Canonical({key:"no-hash"}),semanticEvidenceHash:null});await assert.rejects(()=>s.get("no-hash"),/FINAL_RESULT_WITHOUT_SEMANTIC_HASH/);put("hash-no-final",{...current,attemptId:sha256Canonical({id:"hash-no-final"}),idempotencyKey:sha256Canonical({key:"hash-no-final"}),finalResultStatus:null});await assert.rejects(()=>s.get("hash-no-final"),/SEMANTIC_HASH_WITHOUT_FINAL_RESULT/);});

test("strict operational admission rejects partial and malformed durable records",async()=>{const base=rec("strict");const malformed:unknown[]=[{}, {finalResultStatus:null,semanticEvidenceHash:null}, {...base,attemptId:undefined}, {...base,attemptNumber:-1}, {...base,lifecycle:"BROKEN"}, {...base,authorityGranted:true}, {...base,deactivationAuthorityGranted:true}, {...base,transportOutcome:"BROKEN"}, {...base,createdAt:"not-a-time"}, {...base,expectedIdentity:null}, {...base,requestIdentity:null}, {...base,retry:{retryable:"yes"}}, {...base,preVerificationWatermark:null}, {...base,preRelevantFingerprint:null}, {...base,reasonCodes:{bad:true}}, {...base,failureClassification:"UNKNOWN_FAILURE"}];for(const value of malformed){assert.throws(()=>new InMemoryTargetedVerifierAttemptStore({records:[value as OperationalAttemptRecord]}),/INVALID_OPERATIONAL_RECORD|INVALID_FINAL_RESULT_STATUS|LEGACY_OPERATIONAL_SEMANTIC_HASH_INCOMPATIBLE/);const db=new AttemptDb();const store=new PostgresTargetedVerifierAttemptStore(db);await assert.rejects(()=>store.createOrGet(value as OperationalAttemptRecord),/INVALID_OPERATIONAL_RECORD|INVALID_FINAL_RESULT_STATUS|LEGACY_OPERATIONAL_SEMANTIC_HASH_INCOMPATIBLE/);}});
test("semantic durable identity and journal bindings reject malformed current records",async()=>{const base=rec("semantic");const malformed:unknown[]=[{...base,candidateModelVersion:"wrong"},{...base,generationModelVersion:"wrong"},{...base,verifierSchemaVersion:"wrong"},{...base,verifierPolicyVersion:"wrong"},{...base,providerContractVersion:"wrong"},{...base,normalizerVersion:"wrong"},{...base,requestIdentity:{...base.requestIdentity,orderHash:"0x"+"b".repeat(64)}},{...base,requestIdentity:{...base.requestIdentity,endpointPath:"/wrong"}},{...base,requestIdentity:{...base.requestIdentity,protocolAddress:"bad"}},{...base,expectedIdentity:{...base.expectedIdentity,orderHash:"0x"+"b".repeat(64)}},{...base,expectedIdentity:{...base.expectedIdentity,tokenId:"01"}},{...base,expectedIdentity:{...base.expectedIdentity,protocolAddress:"bad"}},{...base,preRelevantFingerprint:{...base.preRelevantFingerprint,orderHash:"0x"+"b".repeat(64)}},{...base,preVerificationWatermark:{eventId:"x",receivedAt:base.createdAt}},{...base,postVerificationWatermark:{eventId:"1",receivedAt:base.createdAt}},{...base,postRelevantFingerprint:{...base.preRelevantFingerprint,eventIds:["1"],events:[]}},{...base,reasonCodes:{bad:true}},{...base,extraField:"reject"}];for(const value of malformed){assert.throws(()=>new InMemoryTargetedVerifierAttemptStore({records:[value as OperationalAttemptRecord]}));}const db=new AttemptDb();const store=new PostgresTargetedVerifierAttemptStore(db);const raw={...base,requestIdentity:{...base.requestIdentity,orderHash:"0x"+"b".repeat(64)}};db.rows.set("raw-semantic",{payload:clone(raw) as OperationalAttemptRecord,lifecycle:raw.lifecycle,claimedAt:raw.claimedAt,leaseExpiresAt:raw.leaseExpiresAt,leaseToken:raw.leaseToken,nextAttemptAt:raw.nextAttemptAt,failureClassification:raw.failureClassification});await assert.rejects(()=>store.get("raw-semantic"),/INVALID_OPERATIONAL_RECORD/);});
