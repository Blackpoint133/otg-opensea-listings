import fs from 'node:fs'; import crypto from 'node:crypto';
const file=process.argv[2]; if(!file) throw new Error('usage: node scripts/extract_opensea_get_order_schema.mjs OPENAPI.json');
const bytes=fs.readFileSync(file); const full=crypto.createHash('sha256').update(bytes).digest('hex'); const expected='feeb155c9a12fe05e332177014a9d8e7e9ab37fe008c8f37d9bd17c5185cf922'; if(full!==expected) throw new Error(`OPENAPI_SHA_MISMATCH:${full}`);
const doc=JSON.parse(bytes); const schemas=doc.components?.schemas??{}; const seen=new Set(); const walk=(x)=>{if(!x||typeof x!=='object')return;if(typeof x.$ref==='string'&&x.$ref.startsWith('#/components/schemas/')){const n=x.$ref.split('/').pop();if(!seen.has(n)){seen.add(n);walk(schemas[n]);}} for(const v of Object.values(x))walk(v);}; const op=Object.values(doc.paths??{}).flatMap(p=>Object.values(p)).find(x=>x?.operationId==='get_order'); walk(op?.responses?.['200']?.content?.['*/*']?.schema); const out={operationId:'get_order',components:Object.fromEntries([...seen].sort().map(n=>[n,schemas[n]]))}; const canon=JSON.stringify(out); console.log(JSON.stringify({fullSha256:full,components:[...seen].sort(),extractedSha256:crypto.createHash('sha256').update(canon).digest('hex')},null,2));
